const ErrorPage = () => {
  return ( 
    <div>
      <h1>Error!</h1>
      <p>Looks like something went wrong. Please refresh the page.
        This will show up in production only, not development
      </p>
    </div>
   );
}
 
export default ErrorPage;